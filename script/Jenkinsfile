pipeline {
    agent any
    tools {
        maven 'Maven' // This should match the name given in Global Tool Configuration
    }
    stages {
        stage('Build') {
            steps {
                script {
                    // Use the configured Maven tool
                    sh 'mvn clean validate compile install test package'
                }
            }
        }
    }
}